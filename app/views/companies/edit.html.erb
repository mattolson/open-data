<% set_page_title 'Edit Company' -%>

<h1>Edit '<%=h @company.name %>'</h1>
<% semantic_form_for @company, :url => company_path(@company), :method => :put, :html => {:multipart => true} do |form| %>
  <%= render :partial => 'form', :locals => {:form => form} %>
<% end %>
